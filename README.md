# Tráfico112 - Defensa Integral de Víctimas

Este proyecto es una landing page profesional diseñada para informar a las víctimas de accidentes de tráfico sobre el convenio UNESPA y sus derechos legales.

## 🚀 Cómo ejecutar localmente

Al utilizar módulos ES6 nativos y React vía ESM, no necesitas instalar dependencias pesadas.

1. Simplemente abre el archivo `index.html` usando un servidor local.
2. Recomendamos usar la extensión **Live Server** de VS Code o el comando `npx serve` en la raíz del proyecto.

## 📦 Cómo subir a GitHub sin errores

Si estás teniendo problemas para subir el código, sigue estos pasos desde tu terminal en la carpeta del proyecto:

```bash
# 1. Inicializa el repositorio
git init

# 2. Añade todos los archivos
git add .

# 3. Crea el primer commit
git commit -m "Initial commit: Tráfico112 Landing Page"

# 4. Cambia la rama a main
git branch -M main

# 5. Conecta con tu repositorio de GitHub (cambia el URL)
git remote add origin https://github.com/TU_USUARIO/TU_REPOSITO.git

# 6. Sube los archivos
git push -u origin main
```

## 🌐 Despliegue en GitHub Pages

1. Ve a la pestaña **Settings** de tu repositorio en GitHub.
2. Entra en **Pages**.
3. En "Branch", selecciona `main` y la carpeta `/(root)`.
4. ¡Listo! Tu web estará online en unos minutos.
